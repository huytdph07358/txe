name: txe
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 20230707.0.0+20230707 #TODO:BUILD_ENV

environment:
  sdk: '>=2.18.5 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.0.2
  flutter_localizations:
    sdk: flutter
  nb_utils: ^4.5.2
  intl: any
  upgrader: ^6.3.0
  uni_links: ^0.5.1
  uuid: ^3.0.7
  firebase_analytics: ^10.2.0
  firebase_core: ^2.7.1
  firebase_messaging: ^14.2.6
  get: ^4.6.5
  package_info_plus: ^3.0.3
  firebase_crashlytics: ^3.3.2
  carousel_slider: ^4.2.1

  vss_locale:
    path: ../vss_plugins/vss_locale
  vss_global_constant:
    path: ../vss_plugins/vss_global_constant
  vss_environment:
    path: ../vss_plugins/vss_environment
  vss_theme:
    path: ../vss_plugins/vss_theme
  vss_name_util:
    path: ../vss_plugins/vss_name_util
  vss_navigator:
    path: ../vss_plugins/vss_navigator
  vss_ivt_api:
    path: ../vss_plugins/vss_ivt_api
  vss_log:
    path: ../vss_plugins/vss_log

  txe_login:
    path: ./plugins/txe_login
  txe_screen:
    path: ./plugins/txe_screen
  txe_app_constant:
    path: ./plugins/txe_app_constant
  txe_config:
    path: ./plugins/txe_config
  txe_environment_chooser:
    path: ./plugins/txe_environment_chooser
  txe_backend_domain:
    path: ./plugins/txe_backend_domain
  txe_uri:
    path: ./plugins/txe_uri
  txe_change_password:
    path: ./plugins/txe_change_password
  txe_app_store:
    path: ./plugins/txe_app_store
  txe_authorize:
    path: ./plugins/txe_authorize
  txe_dashboard:
    path: ./plugins/txe_dashboard
  txe_pos_integrate:
    path: ./plugins/txe_pos_integrate
  txe_line:
    path: ./plugins/txe_line
  txe_trip:
    path: ./plugins/txe_trip
  txe_ticket_sale:
    path: ./plugins/txe_ticket_sale
  txe_ticket_audit:
    path: ./plugins/txe_ticket_audit
  txe_design_book_seet:
    path: ./plugins/txe_design_book_seet

dev_dependencies:
  flutter_test:
    sdk: flutter
  mobx_codegen: ^2.0.5+2
  build_runner: ^2.1.7
  json_serializable: ^6.6.1
  lint: ^1.8.2
  flutter_expandable_fab: ^1.7.1

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/images/flag/
    - assets/images/bookseat/

