name: txe_trip
description: A new Flutter plugin project.
version: 0.0.1
homepage:

environment:
  sdk: '>=2.18.5 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.0.2
  flutter_localizations:
    sdk: flutter
  intl: any
  build_runner: ^2.1.7
  json_serializable: ^6.6.1

  vss_mobile_util:
    path: ../../../vss_plugins/vss_mobile_util
  vss_date_time_util:
    path: ../../../vss_plugins/vss_date_time_util
  vss_cache_network_image:
    path: ../../../vss_plugins/vss_cache_network_image
  vss_locale:
    path: ../../../vss_plugins/vss_locale
  vss_global_constant:
    path: ../../../vss_plugins/vss_global_constant
  vss_ivt_api:
    path: ../../../vss_plugins/vss_ivt_api
  vss_message_util:
    path: ../../../vss_plugins/vss_message_util

  txe_app_bar:
    path: ../txe_app_bar
  txe_app_store:
    path: ../txe_app_store
  txe_authorize:
    path: ../txe_authorize
  txe_backend_domain:
    path: ../txe_backend_domain
  txe_uri:
    path: ../txe_uri
  txe_ticket_sale:
    path: ../txe_ticket_sale
#  txe_ticket_push:
#    path: ../txe_ticket_push
  txe_ticket_audit:
    path: ../txe_ticket_audit

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  # This section identifies this Flutter project as a plugin project.
  # The 'pluginClass' specifies the class (in Java, Kotlin, Swift, Objective-C, etc.)
  # which should be registered in the plugin registry. This is required for
  # using method channels.
  # The Android 'package' specifies package in which the registered class is.
  # This is required for using method channels on Android.
  # The 'ffiPlugin' specifies that native code should be built and bundled.
  # This is required for using `dart:ffi`.
  # All these are used by the tooling to maintain consistency when
  # adding or updating assets for this project.
  plugin:
    platforms:
      android:
        package: com.vietsens.txe_trip
        pluginClass: TxeTripPlugin
      ios:
        pluginClass: TxeTripPlugin

  # To add assets to your plugin package, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg
  #
  # For details regarding assets in packages, see
  # https://flutter.dev/assets-and-images/#from-packages
  #
  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware

  # To add custom fonts to your plugin package, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts in packages, see
  # https://flutter.dev/custom-fonts/#from-packages
